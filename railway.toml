[build]
builder = "nixpacks"
buildCommand = "npm install && cd server/bridge && npm install && cd ../../dashboard && npm install && npm run build && cd .. && pip install -r server/requirements.txt"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
