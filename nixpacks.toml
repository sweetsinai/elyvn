# Railway deployment config — Node.js bridge server with SQLite
[phases.setup]
nixPkgs = ["nodejs_20", "python3"]

[phases.install]
cmds = ["cd server/bridge && npm ci"]

[start]
cmd = "cd server/bridge && node index.js"
